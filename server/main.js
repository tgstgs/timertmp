import { Meteor } from 'meteor/meteor';
//import { Random } from 

Meteor.startup(() => {
  // code to run on server at startup
  //Meteor.publish('time', function() {
    // tbd
  //});

  // Time = new Mongo.Collection('time'); // Try this later

});
